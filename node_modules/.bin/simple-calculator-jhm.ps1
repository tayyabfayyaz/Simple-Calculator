#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir//usr/bin/evn$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir//usr/bin/evn$exe" node "$basedir/../simple-calculator-jhm/index.js" $args
  } else {
    & "$basedir//usr/bin/evn$exe" node "$basedir/../simple-calculator-jhm/index.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "/usr/bin/evn$exe" node "$basedir/../simple-calculator-jhm/index.js" $args
  } else {
    & "/usr/bin/evn$exe" node "$basedir/../simple-calculator-jhm/index.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
